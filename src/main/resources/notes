Task
1. SQL:
a) id callslog add
2. JDBC, почитать про DAO, generic
a) испоьзуя jdbc запилить 3 класса (generic AbstractEntityDAO (entity persons, callslog) родительский класс; и два наследника PersonsDAO, CallsLogDAO)
должны быть методы по доступы к этим объектам в базе (getbyID sql). Общий интерфейс для сущностей не DAO
3. какая вероятность глянув на часы увидеть любую из этих определнные комбинации часы(24) и минуты(60) данные могут меняться.
a) часы=минуты
b) инверсны 13=31, 12=21
c) кратность минут в 2 или 3 раза больше чем часов, подключаемая функция (можно отключить)
MIN: сделать первый пункт часы=минуты
4. запилить на свинге UI и вывести таблицу звонков;
Questions
1.
