Task
1. SQL: базу на этом компе, и заполнить значениями штук 5 логов, персонав 2-3
a) варчары размер более адекватный
b) enum на инты переделать
c) название таблиц Журнал звонков и Персонс вместо того что есть
d) callerID, addresseID добавить в таблицу callLog
e) primary key foreign key добавить в базу
f) почитать про индексы в базе даных, для чего, плюсы/минусы, когда использовать
2. подрубить jdbc (jar), создать класс с доступом к базе, написать запрос в котором мы достаем из быза нечто.
3. какая вероятность глянув на часы увидеть любую из этих определнные комбинации часы(24) и минуты(60) данные могут меняться.
a) часы=минуты
b) инверсны 13=31, 12=21
c) кратность минут в 2 или 3 раза больше чем часов, подключаемая функция (можно отключить)
Questions
1.
